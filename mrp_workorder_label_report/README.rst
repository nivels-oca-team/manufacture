==========================
MRP Workorder Label Report
==========================

This module allows to define a specific report to be used as product Label
on quality points linked to operations, instead of the default ZPL or PDF choice.
